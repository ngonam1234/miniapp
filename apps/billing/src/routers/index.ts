import { Router } from "express";

// import { router as templateRouter } from "./external/template.router";
export const router: Router = Router();

// router.use(`${configs.api.prefix}/templates`, verifyToken, templateRouter);
